import { connect } from 'react-redux';
import SingleStock from '../components/SingleStock';

const mapStateToProps = store => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(undefined, undefined)(SingleStock);